<Window x:Class="WpfApp3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Width="400">
            <!--<Button Command="{Binding MessageCommand}" FontSize="30" Content="Button 1" ></Button>
            <Button Command="{Binding SendCommand}" FontSize="30" Content="Button Send" ></Button>-->


            <!--<Button Content="Hello" FontSize="30" >
                <Button.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding MessageCommand}" ></MouseBinding>
                </Button.InputBindings>
            </Button>-->


            <TextBox>
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding MessageCommand}" >
                        
                    </KeyBinding>
                </TextBox.InputBindings>
            </TextBox>
            
         
        </StackPanel>
    </Grid>
</Window>
